<li>
    When a participant earns <span class="label label-info">{{ threshold }}</span> for 
    <span class="label label-warning">{{ activity }}</span> in a <span class="label label-info">{{ timePeriod }}</span>
    then they earn <span class="label label-info">{{ bonus }}</span> bonus points.
</li>